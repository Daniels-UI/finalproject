CREATE DATABASE IF NOT EXISTS campusconnect_db;
USE campusconnect_db;

CREATE TABLE IF NOT EXISTS registrations (
    id INT AUTO_INCREMENT PRIMARY KEY,
    full_name VARCHAR(100) NOT NULL,
    email VARCHAR(120) NOT NULL,
    phone VARCHAR(20) NOT NULL,
    student_id VARCHAR(50) NOT NULL,
    event_name VARCHAR(100) NOT NULL,
    attendance_type VARCHAR(30) NOT NULL,
    message TEXT,
    registered_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Sample record for demonstrating data retrieval in php/view_registrations.php
INSERT INTO registrations
(full_name, email, phone, student_id, event_name, attendance_type, message)
VALUES
('Sample Student', 'sample@example.com', '0712345678', 'STU001',
 'Tech Skills Workshop', 'In-person', 'Sample database record.');[database.sql](https://github.com/user-attachments/files/31030844/database.sql)
